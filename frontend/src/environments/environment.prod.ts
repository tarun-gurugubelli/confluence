export const environment = {
  production: true,
  // Set to your AWS Lambda Function URL (overwritten by the GitHub Actions
  // deploy workflow when the API_BASE_URL repository variable is set).
  apiBase: 'API_BASE_URL_PLACEHOLDER',
};
